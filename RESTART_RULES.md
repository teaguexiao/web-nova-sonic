# 🔄 Nova Sonic 服务重启规则

## 自动重启规则

**重要**: 修改以下文件后，**必须重启 nova-sonic 服务**才能生效。

---

## ✅ 需要重启的文件修改

### 后端 Python 代码
- ✅ `main.py` - 主应用逻辑
- ✅ `tools.py` - 工具管理器
- ✅ 任何其他 `.py` 文件
- ✅ `.env` - 环境变量配置

### 前端静态文件
- ✅ `templates/*.html` - HTML 模板
- ✅ `static/js/*.js` - JavaScript 文件
- ✅ `static/css/*.css` - CSS 样式文件
- ✅ `static/` 下的任何静态资源

### 配置文件
- ✅ `requirements.txt` - Python 依赖（需要先安装）
- ✅ 任何配置文件的修改

---

## 🚀 重启方法（三选一）

### 方法 1：使用 Slash Command（推荐）
```bash
/restart-service
```
**优点**: 最简单，Claude Code 会自动执行重启并验证

---

### 方法 2：使用快捷脚本
```bash
cd /home/ubuntu/web-nova-sonic
./restart.sh
```
**优点**: 提供详细的状态输出

---

### 方法 3：手动 systemctl 命令
```bash
sudo systemctl restart nova-sonic
sudo systemctl status nova-sonic
```
**优点**: 最基础的方法

---

## ❌ 不需要重启的修改

以下修改**不需要**重启服务：

- ❌ 查看文件内容（Read 操作）
- ❌ 创建文档文件（如 .md 文档）
- ❌ 修改 git 相关文件（.gitignore 等）
- ❌ 修改日志文件

---

## 🤖 Claude Code 自动化规则

**给 Claude Code 的指示**：

每当修改以下文件后，**主动**调用 `/restart-service` 命令：
- `main.py`
- `tools.py`
- `templates/*.html`
- `static/js/*.js`
- `static/css/*.css`
- `.env`

**执行流程**：
1. 完成文件修改
2. 保存文件
3. 主动调用 `/restart-service`
4. 验证服务状态
5. 通知用户服务已重启

---

## 📋 重启检查清单

重启服务后，确认以下几点：

- [ ] 服务状态为 `Active (running)`
- [ ] 日志显示 `Application startup complete`
- [ ] 端口 8100 正在监听
- [ ] 没有错误日志

---

## 🔍 故障排查

如果重启失败，检查：

1. **查看日志**
   ```bash
   sudo journalctl -u nova-sonic -n 50
   ```

2. **检查 Python 语法**
   ```bash
   source /home/ubuntu/venv-py313/bin/activate
   python -m py_compile main.py
   ```

3. **检查端口占用**
   ```bash
   ss -tlnp | grep 8100
   ```

4. **手动启动测试**
   ```bash
   cd /home/ubuntu/web-nova-sonic
   source /home/ubuntu/venv-py313/bin/activate
   python main.py
   ```

---

## 📝 示例工作流程

```
修改代码 → 保存文件 → /restart-service → 验证服务 → 测试功能
```

**Claude Code 会在修改代码后自动提醒并执行重启！**
